# ست طرق سرّبت بيها بيانات رغم إن سياسات RLS كانت مكتوبة صح

ببني نظام نقاط بيع للمحلات. متعدد المستأجرين، على Supabase وPostgres، حوالي تسعين ميجريشن، وفلوس حقيقية بتعدي فيه كل يوم. على مدى ست أسابيع عملت عليه تلات مراجعات أمنية.

كل ثغرة لقيتها عدّت من سياسات كانت — زي ما هي مكتوبة — **صح**.

ودي الحاجة اللي محدش بيقولهالك لما تفعّل Row Level Security وتحس إنك أمّنت. الـRLS طبقة واحدة. تحتيها نظام صلاحيات Postgres، وفوقيها PostgREST، والتسريبات بتعيش في اللحامات بينهم.

دول الستة، وكل واحدة معاها الإصلاح والطريقة اللي تتأكد بيها إن الإصلاح نزل فعلًا.

---

## ١. المنع من `anon` مش بيعمل حاجة

كتبت السطر ده ومشيت:

```sql
revoke execute on function my_function() from anon;
```

و`anon` فضل ينادي الدالة عادي.

السبب: `create function` بيدّي `EXECUTE` لـ`PUBLIC` افتراضيًا، و`anon` بيورث من `PUBLIC`. فلما بتسحب صلاحية الدور نفسه، الصلاحية الموروثة بتفضل مكانها — وPostgres مابيقولّكش حاجة.

الصح:

```sql
revoke all on function my_function() from public, anon;
grant execute on function my_function() to authenticated;
```

ودوال التريجر مش استثناء من المنحة الافتراضية دي. وهي كمان **مش محتاجة `EXECUTE` عشان تشتغل** — Postgres بينفّذ التريجر كجزء من عملية الجدول مش بصلاحية الدور — يعني المنع مش هيكلّفك حاجة.

لقيت تلات دوال تريجر مكشوفة في مراجعة، وفي المراجعة اللي بعدها لقيت **اتنين كمان كنت أنا عاملهم وأنا بصلّح التلاتة الأولانيين**.

> أي `create function` لازم ينتهي بـ`revoke`.

---

## ٢. المنع على مستوى العمود بيفشل في صمت لو الجدول كله مسموح

سعر الشرا هو الرقم اللي منتجي كله موجود عشان يحميه. الكاشير المفروض مايشوفهوش أبدًا. فكتبت:

```sql
revoke select (buy_price) on items from authenticated;
```

مفيش خطأ. ومفيش أي تأثير.

الدور لسه معاه `SELECT` على الجدول كله، والمنحة على مستوى الجدول بتبلع منح الأعمدة.

الصح:

```sql
revoke select on items from authenticated;
grant select (id, name, sell_price, stock) on items to authenticated;
```

وده ليه تمن لازم تدفعه وانت عارف: `select *` على الجدول ده هيقع بعد كده. كل قراءة لازم تسمّي أعمدتها، وكل عمود جديد لازم يتضاف في المنحة **وفي الاستعلام**. ده عبء صيانة حقيقي، وهو المقايضة الصح.

---

## ٣. العمود الجديد ممنوع قراءة، ومسموح كتابة

دي أكتر حاجة كنت أتمنى أعرفها من سنة:

```
عمود جديد + SELECT  ←  ممنوع  لحد ما تسمح
عمود جديد + UPDATE  ←  مسموح لحد ما تمنع
```

كنت قافل القراءة على جدول عمود عمود. الكتابة فضلت عليها `GRANT UPDATE` على مستوى الجدول. وبعد شهور ضفت عمود عدّاد فواتير على الجدول ده، ماديتش عليه أي منحة، وافترضت إن "ولا حاجة" معناها ولا حاجة.

المتصفح كان يقدر يكتب فيه. اتجرّب بحساب مستخدم حقيقي:

```
العدّاد قبل = ٥٤    بعد = ١    الرد = عدّى من غير أي خطأ
```

الفاتورة الجاية كانت هتبقى رقم ١ تاني — **رقم مكرر في دفتر فواتير ضريبي**، بصمت. اتقفلت بتريجر و`unique index` على `(business_id, invoice_number)` كحزام تاني.

> **أي عمود جديد فيه سؤالين مش سؤال: مين يقرا؟ (منح) ومين يكتب؟ (منح وتريجرات).** الاتنين بيفشلوا في اتجاهين متعاكسين.

---

## ٤. الـview اللي بتتخطى RLS عشان تقرا، بتتخطاها عشان تكتب كمان

كنت محتاج شاشة واحدة تقرا عمود الـRLS مانعاه، فعملت view. في Postgres 15 وفوق، الـview بتشتغل بصلاحيات مالكها إلا لو اتعملت بـ`security_invoker = true` — فالـview شافت اللي المستخدم مش شايفه. وده كان المقصود.

اللي مافكرتش فيه: `select` بسيط من جدول واحد بيبقى **قابل للتعديل تلقائيًا**، وPostgREST بيعرض عليه `INSERT` و`UPDATE` و`DELETE` زي أي جدول.

اتجرّب بحساب موظف حقيقي:

```
delete from items       ←  ٠ صفوف    (الـRLS شغالة)
delete from item_costs  ←  ١ صف      (عدّى)
```

موظف كان يقدر يمسح أي صنف في المحل من خلال view أنا عملتها عشان **يقرا** سعر.

```sql
revoke all on item_costs from authenticated, anon;
grant select on item_costs to authenticated;
```

واللي أنقذ الموقف جزئيًا: تريجر حماية أعمدة رفض تغيير رقم المحل الجاي من خلال الـview. **التريجرات بتشتغل حتى لو الكتابة جاية من view.** الحذف مالوش تريجر، وده بالظبط اللي خلاه يعدّي.

> لو بتعتمد على التريجرات كخط دفاع أخير، غطّي الحذف كمان.

---

## ٥. `auth.role()` مش بتعرف الكتابة جاية منين

كتبت تريجر يمنع المتصفح يتلاعب في عدّاد، وحطيت الشرط `auth.role() = 'authenticated'`.

وده كان هيوقّف **كل بيعة في البرنامج**.

السبب: `auth.role()` بتقرا من الـJWT، والـJWT مابيتغيّرش لما تدخل جوه دالة `SECURITY DEFINER`. يعني جوه `record_sale()` بتاعتي — اللي أنا كاتبها ومراجعها — فضلت بترجّع `authenticated`، فالتريجر منع المسار الشرعي مع غير الشرعي.

اللي بيعرف هو `current_user`. PostgREST بيشتغل بدور `authenticated`؛ وجوه دالة `SECURITY DEFINER` مملوكة لـ`postgres` بيبقى `postgres`.

> `auth.role()` بتقول **مين المستخدم**.
> `current_user` بتقول **الكتابة جاية منين**.

---

## ٦. الـRLS بتجاوب على السؤال اللي سألته، مش اللي قصدته

صنفين في محل عميل كان مخزونهم مش مساوي مجموع دفعاتهم. الدفعات الزيادة كانت برقم محلي **أنا**، على أصناف **بتاعته هو**.

كان عندي دور أدمن بيقرا عبر كل المحلات. فتحت صنف من محله وعدّلت كميته، والكود كتب دفعة مخزون مختومة برقم محلي أنا على صنف مش بتاعي.

والـRLS ماوقفتهاش، وكانت على حق. الـpolicy سألت: *الدفعة دي من محلك؟* آه. **ومحدش سأل: والصنف اللي فيها من محلك كمان؟**

> أي صف بيربط جدولين محتاج policy تسأل عن **الاتنين**، مش عن الصف.

---

## اللي بيلاقي الحاجات دي فعلًا

قراءة الـpolicies مالقيتش ولا واحدة من الستة. تلات حاجات هي اللي لقتهم.

**اسأل الكتالوج.** أغلب اللي فوق بيسيب أثر في `pg_proc` أو `pg_policies` أو `pg_class` أو `information_schema.role_table_grants`. حطيت الفحوصات في سكريبت واحد بيتشغّل بعد كل ميجريشن، ومسك دالة سايبة من إصلاح متسرّع اتعمل في محرر SQL: `SECURITY DEFINER`، من غير `search_path`، من غير أي تحقق هوية، و`anon` يقدر يناديها، وبتكتب في عمود تكلفة. مش مستعملة في أي مكان في الكود. قاعدة كده شهور.

> أي حاجة تتعمل في محرر SQL بره الميجريشنز مابتاخدش مراجعة، ومابتظهرش في أي diff، وبتفضل موجودة للأبد. امسح شغلك المؤقت في نفس الجلسة اللي عملته فيها.

**كون انت المستخدم.** حط بيانات JWT لحساب حقيقي قليل الصلاحيات، وبعدين اعمل `count(*)` على كل جدول في القاعدة وبصّ على اللي رجّع صفوف:

```sql
select set_config('request.jwt.claims',
  '{"sub":"<الـauth uid بتاع المستخدم>","role":"authenticated"}', true);
select set_config('role', 'authenticated', true);
-- وبعدين لف على pg_class واعمل count على كل جدول
```

دقيقتين من ده لقولي حاجتين قراءة الكود مالقتهمش: إيميل صاحب المحل، ورقم سري معلّق، الاتنين مكشوفين لحساب كاشير.

**صدّق القاعدة مش الميجريشن.** أوحش ساعة ضاعت مني كانت لما افترضت إن باب مقفول لأن التطبيق بطّل يخبط عليه. الكود بطّل ينادي دالة؛ الدالة فضل عليها `grant execute ... to authenticated`، وفضلت ترجّع بيانات التكلفة لأي حد يناديها مباشرة. والأوحش: نداءها كان بيخصم من المخزون **من غير فاتورة** — يعني موظف يقدر يمشّي بضاعة والجرد يطلع ناقص ومحدش يعرف ليه.

> **الكود اللي بطّل يستعمل باب مابيقفلوش.**

بعد أي منع، اسأل القاعدة:

```sql
select has_function_privilege('anon', 'public.my_func()', 'execute');
```

«نفّذت الأمر» مش دليل.

---

## السكريبت

فحوصات الكتالوج كلها في ملف واحد تنسخه في محرر SQL بتاع Supabase وتدوس Run. كله قراءة — مابيغيّرش ولا صف — وأي رقم فوق الصفر عايز نظرة:

**[supabase-rls-audit.sql](./supabase-rls-audit.sql)**

لو شغّلته وطلعلك رقم مش صفر ومش عارف تفسّره، ابعتلي النتيجة وأنا أبصّلك عليها. ده شغلي.
